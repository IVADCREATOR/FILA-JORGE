# Caminho do arquivo SQLite (nunca dentro de pasta servida publicamente)
DATABASE_PATH=./data/sorasaki.db

# Porta da API
PORT=3000

# Ambiente
NODE_ENV=development

# Usados nas próximas etapas (auth), já deixando reservado:
SESSION_SECRET=changeme-generate-a-real-random-secret
